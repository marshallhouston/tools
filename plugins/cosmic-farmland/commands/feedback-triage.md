---
description: Triage a feedback blob from a named source into structured items + auto-spawn worktrees for accepted work
argument-hint: "<source-name> (then paste feedback)"
---

Invoke the `feedback-triage` skill.

If `$ARGUMENTS` is provided, treat it as the source name. Otherwise ask for source + feedback blob.

Follow the skill's 7-step process exactly. Do not skip the pre-write triage table confirmation.
